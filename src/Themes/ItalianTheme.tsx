import { createTheme } from '@mui/material/styles';

export const ItalianTheme = createTheme({
    palette: {
        primary: {
            main: '#b63122',
        },
        secondary: {
            main: '#006500',
        },
    },
});